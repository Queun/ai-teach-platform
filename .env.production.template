# ==========================================
# AI教育平台 - 生产环境配置模板
# 请根据你的服务器环境修改以下配置
# ==========================================

# 数据库配置
DATABASE_USERNAME=strapi
DATABASE_PASSWORD=your_secure_password_here
DATABASE_PORT=5432

# Strapi后端配置
STRAPI_PORT=1337

# 生产环境密钥 - 必须修改为强随机值！
STRAPI_APP_KEYS=key1,key2,key3,key4
STRAPI_API_TOKEN_SALT=your_api_token_salt_here
STRAPI_ADMIN_JWT_SECRET=your_admin_jwt_secret_here
STRAPI_TRANSFER_TOKEN_SALT=your_transfer_token_salt_here
STRAPI_JWT_SECRET=your_jwt_secret_here
STRAPI_ENCRYPTION_KEY=your_encryption_key_here

# 前端配置
FRONTEND_PORT=3000
NEXT_PUBLIC_STRAPI_URL=/api
NEXT_PUBLIC_STRAPI_TOKEN=your_public_token_here
STRAPI_API_TOKEN=your_api_token_here

# 可选：Nginx配置
NGINX_PORT=80

# ==========================================
# 安全提醒
# ==========================================
# 1. 务必修改所有密钥为强随机值
# 2. 使用复杂密码作为数据库密码
# 3. 不要将此文件提交到版本控制系统
# 4. 在生产环境中限制文件权限: chmod 600 .env.production

# ==========================================
# 密钥生成建议
# ==========================================
# 可以使用以下命令生成强密钥：
# openssl rand -base64 32
# 或在Node.js中：
# require('crypto').randomBytes(32).toString('base64')

# ==========================================
# 使用方法
# ==========================================
# 1. 复制此文件为 .env 或 .env.production
# 2. 修改所有配置值
# 3. 在docker compose命令前加载环境变量：
#    source .env.production && docker compose -f docker-compose.prod.yml up -d